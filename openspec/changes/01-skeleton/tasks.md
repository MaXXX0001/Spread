# Tasks: 01-skeleton

Скорочення в перевірках: `W` означає `docker exec -u laradock -w /var/www/Spread laradock_all-workspace-85-1`.
Гілки: реалізація йде на `feature/01-skeleton`, кожна задача з кодом (групи 2–3) — в окремій `task/01-skeleton/<what>` (назву вказано в дужках). Задачі без коду (групи 1 і 4) виконуються прямо на feature-гілці або поза репозиторієм. Git-команди виконує користувач.

## 1. Інфраструктура (не код)

- [x] 1.1 [агент, з дозволу користувача] Створити в postgres Laradock бази `spread` і `spread_testing`: `docker exec laradock_all-postgres-1 psql -U default -d postgres -c 'CREATE DATABASE spread'` і те саме для `spread_testing`. Перевірка: `docker exec laradock_all-postgres-1 psql -U default -d postgres -tAc "SELECT datname FROM pg_database WHERE datname LIKE 'spread%'"` виводить обидві назви.
  - Виконано 2026-09-24: обидві бази створено, перевірочний запит повертає `spread` і `spread_testing`.
- [x] 1.2 [РУЧНО, користувач] Створити `/home/max/projects/laradock/nginx/sites/85_spread.conf` із вмістом з design.md, розділ D9, і перезавантажити nginx: `docker exec laradock_all-nginx-1 nginx -t && docker exec laradock_all-nginx-1 nginx -s reload`. Перевірка: `nginx -t` пише `syntax is ok`; після задачі 2.1 `curl -sI -H 'Host: spread.local' http://localhost/` з WSL повертає `200`.
  - Конфіг створив агент за дозволом користувача (2026-09-24), за зразком сусідніх сайтів: додано `fastcgi_buffers`, `fastcgi_read_timeout` і блок `/.well-known/acme-challenge/`. `nginx -t` пройшов, nginx перезавантажено, сайти voyager і fisker відповідають 200. Лишилася перевірка на 200 після задачі 2.1.
  - Після задачі 2.1 (2026-09-24) `curl -sI -H 'Host: spread.local' http://localhost/` повертає `HTTP/1.1 200 OK`.
- [ ] 1.3 [РУЧНО, користувач] Додати рядок `127.0.0.1 spread.local` у `C:\Windows\System32\drivers\etc\hosts` (редактор з правами адміністратора). Перевірка: у Windows `ping spread.local` резолвиться в `127.0.0.1`.
- [x] 1.4 З'ясувати, чому в `laradock_all-php-fpm-85-1` немає розширень `redis` і `pcntl`, і усунути прогалину з redis (див. design.md, D8).
  - Причина: у `laradock/php-fpm/Dockerfile` блок «PHP REDIS EXTENSION» був порожній. Його відновлено (бекап `php-fpm/Dockerfile.bak-2026-09-24`), образ перебудовано, контейнер перестворено (2026-09-24, з дозволу користувача).
  - Результат: PHP 8.5.10 + phpredis 6.3.0, `ping` до `redis` працює, сайт voyager відповідає 200. Predis не потрібен. pcntl у php-fpm вимкнено свідомо, бо Horizon працює у workspace-85.

## 2. Каркас застосунку (код)

- [x] 2.1 (`task/01-skeleton/install-laravel`) Встановити Laravel 13 за design.md, D1–D3: `W composer create-project laravel/laravel /var/www/spread-install "^13.0"`, скопіювати вміст у `/var/www/Spread` (`cp -a /var/www/spread-install/. /var/www/Spread/`), видалити тимчасову теку і `database/database.sqlite`. У `.env` і `.env.example` налаштувати pgsql (`postgres`/`spread`/`default`), `REDIS_CLIENT=phpredis`, `REDIS_HOST=redis`, `QUEUE_CONNECTION=redis`, `APP_URL=http://spread.local`, `SPREAD_TRACKER_URL=http://spread.local`. У `phpunit.xml` вказати `DB_CONNECTION=pgsql` і `DB_DATABASE=spread_testing`. Переконатися, що timezone — `UTC`. Перевірка: `git status` показує, що `docs/` і `openspec/` не змінено; `W php artisan --version` виводить `Laravel Framework 13.x`; `W php artisan migrate` проходить; `W php artisan about` показує `Database: pgsql`, `Queue: redis`; `W php artisan test` зелений.
  - Встановлено Laravel 13.33.0 (2026-09-24). Каркас також приніс `AGENTS.md` і `CLAUDE.md` (інструкції Laravel Boost), їх скопійовано як є.
- [x] 2.2 (`task/01-skeleton/filament-panel`) Встановити Filament 5 (`W composer require filament/filament:"^5.0"`, `W php artisan filament:install --panels`), панель `admin` на `/admin` з `->login()`, без registration і password reset. `User` реалізує `FilamentUser` (`canAccessPanel` → `true`), див. D4. Feature-тести за spec admin-access: гостя з `/admin` переадресовує на логін; `/admin/register` → 404; вхід з правильним паролем успішний, з неправильним — ні. Перевірка: `W php artisan test --filter=Admin` зелений; `W php artisan route:list --path=admin` не містить `register` і `password-reset`.
  - Встановлено Filament 5.8.4 (2026-09-24), `filament:install --panels --no-interaction` створив панель `admin` з `->login()`. Інсталятор додав `public/{css,js,fonts}/filament` у `.gitignore` і `filament:upgrade` у `post-autoload-dump`, тож ассети публікуються при `composer install`. `bootstrap/providers.php` інсталятор переписав на FQCN, повернуто `use`-імпорти (вимога pint). Тести — `tests/Feature/AdminAccessTest.php`. Через nginx `/admin/login` і ассети Filament відповідають 200, `/admin/register` — 404.
- [x] 2.3 (`task/01-skeleton/horizon`) Встановити Horizon (`W composer require laravel/horizon:"^5.0"`, `W php artisan horizon:install`). Перевизначити `authorization()` у `App\Providers\HorizonServiceProvider` на «лише автентифікований користувач», див. D4. Feature-тести: гість на `/horizon` → 403, залогінений користувач → 200. Перевірка: `W php artisan test --filter=Horizon` зелений; `W php artisan horizon` стартує без помилок (зупинити Ctrl+C); `W php artisan horizon:status` під час роботи показує `running`.
  - Виконано 2026-09-24: Horizon v5.50.0. `php artisan horizon` запускався у фоні (`docker exec -d`), `horizon:status` показав `running`, зупинено через `horizon:terminate` замість Ctrl+C; `pgrep -af horizon` після цього порожній. `curl` з WSL на `/horizon` для гостя повертає 403 (APP_ENV=local). Додано тест гостя в середовищі `local`. `bootstrap/providers.php` інсталятор знову переписав на FQCN, повернуто `use`-імпорти.

## 3. Довідники й трекінгове посилання (код)

- [x] 3.1 (`task/01-skeleton/traffic-sources`) Міграція `traffic_sources` (D5), модель `TrafficSource` (cast `macros` → array), Filament-ресурс зі списком, створенням, редагуванням і видаленням. Макроси вводяться через `Repeater` (`param`, `macro`) з валідацією за spec traffic-sources: regex назви параметра, унікальність у межах джерела, непорожній макрос без пробілів до 255 символів, мінімум одна пара, унікальна назва джерела. Видалення джерела з кампаніями блокується з повідомленням; тест на це додається в задачі 3.4, коли з'явиться таблиця `campaigns`. Перевірка: `W php artisan test --filter=TrafficSource` зелений (Livewire-тести сторінок ресурсу: створення з двома парами і збереження порядку, кожен випадок невалідних даних, редагування, видалення).
  - Виконано 2026-09-24: 17 тестів у `tests/Feature/TrafficSourceResourceTest.php`. `macros` зберігається як jsonb-список `[{"param": "...", "macro": "..."}]`: порядок пар зберігається, а ключі всередині пари jsonb упорядковує сам (`macro` перед `param`), тому тести порівнюють через `assertEquals`. Блокування видалення джерела з кампаніями (дія видалення, зокрема масова `DeleteBulkAction`, і FK) лишено на задачу 3.4, бо таблиці `campaigns` ще немає.
- [x] 3.2 (`task/01-skeleton/cpa-networks`) Міграція `cpa_networks`, модель `CpaNetwork`, Filament-ресурс (назва — обов'язкова й унікальна, нотатки — необов'язкові). Видалення мережі з офферами блокується; тест на це додається в задачі 3.3. Перевірка: `W php artisan test --filter=CpaNetwork` зелений (створення, створення без нотаток, порожня й дубльована назва, редагування, видалення).
  - Виконано 2026-09-24: 8 тестів у `tests/Feature/CpaNetworkResourceTest.php` (додатково — назва понад 255 символів і збереження без зміни назви). У списку видно назву й нотатки (обрізані до 50 символів). Блокування видалення мережі з офферами лишено на задачу 3.3, бо таблиці `offers` ще немає.
- [ ] 3.3 (`task/01-skeleton/offers`) Міграція `offers` (FK на `cpa_networks` з `restrictOnDelete`), модель `Offer`, Filament-ресурс: select мережі, `url_template` з валідацією за spec offers (http/https, до 2048 символів, обов'язковий `{click_id}`), у списку видно назву мережі. Тест: мережу з оффером не можна видалити. Перевірка: `W php artisan test --filter='Offer|CpaNetwork'` зелений, зокрема тест, що шаблон `https://offer.example/lp?sub1={click_id}&sub2={zone}` читається з БД символ у символ.
- [ ] 3.4 (`task/01-skeleton/campaigns`) Міграція `campaigns` (unique `alias` string(10), FK з `restrictOnDelete`, `active` default true), модель `Campaign` з генерацією alias у `creating` (D6), Filament-ресурс зі списком (назва, джерело, оффер, активність, alias), створенням, редагуванням (alias лише для читання) і видаленням. Тести: формат `^[a-z0-9]{10}$`; alias не змінюється при оновленні й не приймається з форми; 200 кампаній мають різні alias; відсутні джерело або оффер відхиляються; джерело чи оффер, до яких прив'язана кампанія, не видаляються. Перевірка: `W php artisan test --filter='Campaign|TrafficSource|Offer'` зелений.
- [ ] 3.5 (`task/01-skeleton/tracking-link`) Додати `config/spread.php` з `tracker_url` (D7), метод `Campaign::trackingUrl()` і сторінку `ViewCampaign` з infolist, де `TextEntry` посилання має `->copyable()`. Unit- і feature-тести за сценаріями spec campaigns «Трекінгове посилання»: точний рядок для `https://trk.example` і пар `zone`/`creative`, base URL з кінцевим `/`, макроси без URL-кодування, зміна макросів джерела змінює посилання, сторінка перегляду містить посилання. Перевірка: `W php artisan test --filter=TrackingUrl` зелений; `W php artisan test` повністю зелений; `W ./vendor/bin/pint --test` без зауважень.

## 4. Ручна перевірка «Готово, коли» (користувач)

- [ ] 4.1 [РУЧНО, користувач] Створити користувача: `docker exec -it -u laradock -w /var/www/Spread laradock_all-workspace-85-1 php artisan make:filament-user`. У браузері Windows відкрити `http://spread.local/admin`, увійти й пройти шлях: створити джерело «Test Push» з парами `zone` → `{zoneid}` і `creative` → `{bannerid}`; CPA-мережу «Test CPA»; оффер з шаблоном `https://example.com/?sub1={click_id}&sub2={zone}`; кампанію з цим джерелом і оффером. На сторінці перегляду кампанії натиснути кнопку копіювання і вставити результат у Блокнот. Перевірка: вставлено `http://spread.local/c/<10 символів a-z0-9>?zone={zoneid}&creative={bannerid}`. Якщо браузер блокує копіювання на http, див. design.md, Risks.
- [ ] 4.2 [РУЧНО, користувач] Запустити `W php artisan horizon`, у тому ж браузері відкрити `http://spread.local/horizon`: дашборд показує статус `Active`. У вікні інкогніто той самий URL дає 403. Перевірка: обидва результати збігаються з очікуваними.
