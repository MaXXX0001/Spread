# Tasks: 02-redirect

Скорочення в перевірках: `W` означає `docker exec -u laradock -w /var/www/Spread laradock_all-workspace-85-1`, `R` — `docker exec laradock_all-redis-1 redis-cli`, `P` — `docker exec laradock_all-postgres-1 psql -U default -d spread -tAc`.
Гілки: реалізація йде на `feature/02-redirect`, кожна задача з кодом (групи 2–5) — в окремій `task/02-redirect/<what>` (назву вказано в дужках). Задачі без коду (групи 1, 6, 7) виконуються на feature-гілці або поза репозиторієм. Git-команди виконує користувач.
Кожна задача з кодом закінчується тим, що `W php artisan test` і `W ./vendor/bin/pint --test` зелені; нижче вказано лише специфічні перевірки. Уточнення контракту (design.md, «Уточнення контракту») уже внесено в docs/contract.md разом з артефактами цього change; код має їм відповідати.

## 1. Інфраструктура (не код)

- [x] 1.1 [агент з дозволу користувача або РУЧНО] У `/home/max/projects/laradock/nginx/sites/85_spread.conf`, у блок `location ~ \.php$` після `include fastcgi_params;`, додати `fastcgi_param HTTP_X_REAL_IP $remote_addr;` (design.md, D9), потім `docker exec laradock_all-nginx-1 nginx -t && docker exec laradock_all-nginx-1 nginx -s reload`. Перевірка: `nginx -t` пише `syntax is ok`; сусідні сайти відповідають як раніше; після задачі 3.1 — `curl -s -o /dev/null -H 'Host: spread.local' -H 'X-Real-IP: 1.2.3.4' 'http://localhost/c/<alias активної кампанії>'`, потім `R XREVRANGE spread:clicks + - COUNT 1` показує `ip`, відмінний від `1.2.3.4` (адреса Docker-мережі).
  - Рядок додав агент з дозволу користувача 2026-09-24, `nginx -t` пройшов, nginx перезавантажено. Перевірено тимчасовим PHP-файлом: на запит із `X-Real-IP: 6.6.6.6` PHP отримує `172.18.0.1`, тобто заголовок клієнта перезаписано. Задачу відмічу після перевірки на `/c/{alias}` у 3.1.
  - Перевірено в 3.1: запит на `/c/{alias}` через nginx із `X-Real-IP: 6.6.6.6` дав клік з `ip` `172.18.0.1`.
- [ ] 1.2 [РУЧНО, з дозволу користувача] Встановити навантажувальний інструмент у WSL (design.md, D15). Пропозиція: `mkdir -p ~/.local/bin && curl -sSL -o ~/.local/bin/hey https://hey-release.s3.us-east-2.amazonaws.com/hey_linux_amd64 && chmod +x ~/.local/bin/hey`. Альтернатива — `sudo apt install apache2-utils` (`ab`). Потрібно лише перед групою 6. Перевірка: `hey -h` (або `ab -V`) виводить довідку.

## 2. Redis-з'єднання й знімок конфігу (код)

- [x] 2.1 (`task/02-redirect/alias-where-operator`) У `app/Models/Campaign.php`, `generateUniqueAlias()`, замінити `->where('alias', $alias)` на `->where('alias', '=', $alias)` (правило query builder із CLAUDE.md). Перевірка: `W php artisan test --filter=Campaign` зелений; `git diff` містить лише цей рядок.
  - Виконав координатор напряму (однорядкова правка): `--filter=Campaign` зелений, pint чистий.
- [x] 2.2 (`task/02-redirect/redis-connection`) Додати Redis-з'єднання `spread` у `config/database.php` (D1), ключі `fallback_url` і `geoip_path` у `config/spread.php` (D2), `SPREAD_FALLBACK_URL` у `.env.example` і локальний `.env`, `SPREAD_REDIS_DB=15` і `SPREAD_FALLBACK_URL=https://fallback.test/` у `phpunit.xml`. Тест: ключ, записаний через з'єднання `spread`, у Redis має рівно ім'я `spread:...` без префікса (перевірити `KEYS`/`EXISTS` через сирий клієнт із вимкненим префіксом або `getOption(Redis::OPT_PREFIX)` порожній), а номер бази в тестах — 15. Перевірка: `W php artisan test --filter=SpreadRedis` зелений; `W php artisan tinker --execute="Illuminate\Support\Facades\Redis::connection('spread')->set('spread:probe', '1');"`, потім `R EXISTS spread:probe` → `1`, `R DEL spread:probe`.
- [x] 2.3 (`task/02-redirect/config-snapshot`) Будівник знімка в `App\ConfigSnapshot\`, observer з `ShouldHandleEventsAfterCommit` на `Campaign` і `Offer` (`saved`, `deleted`), команда `spread:config:rebuild` (D3). Тести за spec config-snapshot: вміст запису кампанії й `offer_url` символ у символ; `active` false; meta (`v`, формат `generated_at`, `version` зростає, `fallback_url`); нуль кампаній → hash відсутній, meta є; видалена кампанія зникає, інші на місці; `spread:config:campaigns:tmp` після перебудови немає; створення кампанії через Filament-сторінку одразу оновлює знімок; зміна шаблону оффера оновлює обидві його кампанії; відкат `DB::transaction` не змінює знімок; команда відновлює видалені ключі й виводить кількість; порожній `SPREAD_FALLBACK_URL` → ненульовий код, знімок не змінено. Перевірка: `W php artisan test --filter=ConfigSnapshot` зелений; на локальній БД `W php artisan spread:config:rebuild` завершується з кодом 0, `R HGETALL spread:config:campaigns` показує наявні кампанії, `R GET spread:config:meta` — JSON з `fallback_url`.
  - У локальній БД `spread` кампаній немає (0), тож `spread:config:rebuild` вивів `0 campaigns`, `HGETALL` порожній, meta з `fallback_url` є. Запис кампанії перевірено тестами й одноразовим прогоном на `spread_testing` + Redis DB 15 (транзакцію БД відкочено, ключі видалено).

## 3. Редірект (код, гарячий шлях)

- [x] 3.1 (`task/02-redirect/redirect-endpoint`) Модуль `app/HotPath/`, файл `routes/hot-path.php`, реєстрація в `bootstrap/app.php` без стеку `web`, виняток `c/*` для режиму обслуговування (D4–D8). Unit-тести: розбір сирого рядка запиту (звичайні, повторювані, `+`/`%XX`, `a.b` і `sub[1]`, порожнє ім'я, пара без `=`, некоректний UTF-8, 60 параметрів → 50, ім'я 100 → 64, значення 5000 → 1024 символи, багатобайтні символи); підстановка (`{click_id}` навіть за наявності параметра `click_id`, `a b&c` → `a%20b%26c`, відсутній параметр, регістр, `{не плейсхолдер}` лишається). Feature-тести за spec redirect: кожен рядок таблиці поведінки (знімок заповнюється прямо в Redis DB 15), `HEAD` на активну й вимкнену кампанію → той самий 302 і `Location` (без `click_id`-частини), що й `GET`, `XLEN spread:clicks` не змінився, `Cache-Control` містить `no-store`, немає `Set-Cookie`, режим обслуговування, БД недоступна (клас тесту без `RefreshDatabase`, хибний host у конфігу pgsql + `DB::purge()`), недоступний Redis (хибний порт з'єднання `spread`) → 302 на `SPREAD_FALLBACK_URL`, формат повідомлення (усі поля, `params` порожній → `{}`), час ULID = `ts`, UA 3000 → 1024, 1000 різних click_id. Перевірка: `W php artisan test --filter='HotPath|Redirect'` зелений; `W php artisan route:list --path=c` показує маршрут без middleware `web`; через nginx `curl -s -o /dev/null -D - -H 'Host: spread.local' 'http://localhost/c/<alias>?zone=1'` (`GET`) → `302`, `Location` на оффер, `R XLEN spread:clicks` зріс на 1; `curl -sI` з тим самим URL (`HEAD`) → `302` з тим самим оффером, `XLEN` не змінився.
  - nginx-перевірки зроблено на тимчасовому записі `tmp-agent-31` у знімку локального Redis DB 0 (без рядка в БД); після перевірки стрім `spread:clicks` (його до того не було) видалено, знімок перебудовано `spread:config:rebuild`.
- [x] 3.2 (`task/02-redirect/hot-path-guard`) Тест-охоронець ізоляції `tests/Unit/HotPathIsolationTest.php` (D5): сканує `app/HotPath/**/*.php` і `routes/hot-path.php`, падає з назвою файлу й рядка на `App\` поза `App\HotPath\`, `Illuminate\Database\`, фасади `DB`/`Schema`, `Eloquent`; містить самоперевірку регулярного виразу на рядку `use App\Models\Campaign;`. Перевірка: `W php artisan test --filter=HotPathIsolation` зелений; тимчасово додати `use App\Models\Campaign;` у файл модуля → тест червоний з назвою файлу; прибрати.
  - Негативну перевірку зроблено у `app/HotPath/OfferUrl.php` (`use App\Models\Campaign;` + `use Illuminate\Support\Facades\DB;` → тест назвав `OfferUrl.php:5` і `:6`) і в `routes/hot-path.php` (`\DB::`, `Eloquent`); обидва файли відновлено з копії. Голі `DB::`/`Schema::` (аліаси фасадів) теж ловляться.

## 4. Таблиця кліків, воркер і збагачення (код)

- [x] 4.1 (`task/02-redirect/clicks-table`) Міграція `clicks` (D10: PK `ulid('click_id')`, `timestamp('clicked_at', 3)`, `inet`, `jsonb`, прапорці, три індекси, без FK), модель `App\Models\Click` (D14) і `Campaign::clicks()`. Тест: запис і читання кліку з `params` `{"zone": "{zoneid}", "creative": ""}` повертає ті самі пари; `clicked_at` зберігає мілісекунди; дубль `click_id` відхиляється БД. Перевірка: `W php artisan migrate` проходить; `W php artisan test --filter=ClickModel` зелений; `P "\d clicks"` показує типи з D10 та індекси.
- [x] 4.2 (`task/02-redirect/clicks-consumer`) Команда `spread:clicks:consume` і клас обробки в `App\Clicks\` (D11, D12) — поки без збагачення (поля збагачення null, прапорці false). Тести за spec click-processing: група створюється, `BUSYGROUP` ігнорується; 100 повідомлень → 100 кліків, `XLEN` 0, pending порожній; повтор того самого повідомлення → один запис; повідомлення, забране іншим consumer і не підтверджене, повертається через `XAUTOCLAIM` (у тесті — з `min-idle` 0 через параметр класу або `XCLAIM` для штучного старіння); невалідний JSON, `v` 2, бракує `click_id`, `payload` відсутній → `spread:clicks:dead` з `reason`; змішана пачка 9 + 1; `ip` `""` → null; помилка БД (хибний host + `DB::purge()`) → повідомлення лишаються в pending, виняток не виходить з ітерації. Перевірка: `W php artisan test --filter=ClickConsumer` зелений; вручну: `W php artisan spread:clicks:consume` у одному терміналі, `curl` на редірект у іншому → `P "select count(*) from clicks"` зріс, `R XLEN spread:clicks` → 0; `docker exec laradock_all-workspace-85-1 pkill -TERM -f spread:clicks:consume` → процес завершився протягом ~2 с з кодом 0.
  - Тести викликають `ClickConsumer::iterate()` напряму (D11), `min-idle` — параметр конструктора; сама команда лише крутить цикл, опції для обмеження ітерацій немає. Ручна перевірка — на тимчасовому записі `tmp-agent-42` у знімку DB 0: 3 запити через nginx → 3 кліки в `spread`.clicks, `XLEN` і pending 0; після `pkill -TERM` процес завершився за ~2 с з кодом 0. Потім рядки видалено, стрім `spread:clicks` (його до того не було) видалено, знімок перебудовано.
- [x] 4.3 (`task/02-redirect/device-detection`) `W composer require matomo/device-detector` (немає версії під PHP 8.5 — зупинитися й спитати користувача). Визначення `device_type`, ОС, браузера й `is_bot` у воркері з кешем за UA в межах пачки (D13). Тести: Chrome на Android → `smartphone`/`Android`/браузер Chrome, не бот; Googlebot → бот; `user_agent` null і `""` → бот, решта полів null; десктопний Firefox на Windows → `desktop`/`Windows`/`Firefox`. Перевірка: `W php artisan test --filter='DeviceDetect|ClickConsumer'` зелений.
  - Встановлено `matomo/device-detector` 6.5.1. Один `DeviceDetector` на воркер (`discardBotInformation()`; розібрані YAML-регекспи в `StaticCache` процесу), кеш за UA — масив у `process()`. Для ботів пристрій/ОС/браузер null. Версія браузера обрізана бібліотекою до minor (`128.0`). Тести: `tests/Unit/DeviceDetectionTest.php` + сценарій пачки в `ClickConsumerTest`.
- [x] 4.4 (`task/02-redirect/geoip`) `W composer require maxmind-db/reader`; команда `spread:geoip:update` (поточний місяць UTC, при 404 — попередній; атомарна заміна; D13); визначення країни у воркері (файл відкривається на старті, немає файлу → попередження один раз і null; `ZZ` → null); рядок атрибуції DB-IP у `README.md`. Тести: команда з `Http::fake()` (успіх, 404 поточного місяця → попередній, помилка → ненульовий код і старий файл цілий); `8.8.8.8` → `US` на справжньому файлі (тест `markTestSkipped`, якщо файлу немає); `172.18.0.1` → null; немає файлу → клік записано з country null. Перевірка: `W php artisan spread:geoip:update` завантажує файл у `storage/app/geoip/`, `git status` його не показує; `W php artisan test --filter='GeoIp|ClickConsumer'` зелений без пропущених гео-тестів.
- [ ] 4.5 (`task/02-redirect/duplicate-flag`) Позначка дублів одним `UPDATE` після вставки пачки, константа `DUPLICATE_WINDOW_SECONDS = 60` (D13). Тести за spec click-processing «Позначка дубля»: різниця 10 с → другий дубль; 120 с → жоден; інший UA → жоден; обидва UA null → другий дубль; пізніший оброблено окремою пачкою раніше за раніший → позначено саме пізніший; ip null → не позначається; кліки в межах однієї пачки. Перевірка: `W php artisan test --filter=Duplicate` зелений.

## 5. Адмінка (код)

- [ ] 5.1 (`task/02-redirect/clicks-admin`) Filament-ресурс `Clicks` лише для читання (D14): список з колонками й фільтром за кампанією, сортування від нових, сторінка перегляду з усіма полями й `params` як парами. Livewire-тести за spec clicks-admin: рядок з назвою кампанії, країною, пристроєм, ОС, браузером; порядок за часом; фільтр за кампанією; прапорці; гість → логін; `/admin/clicks/create` → 404; на сторінках немає дій create/edit/delete; сторінка перегляду показує `{bannerid}` як є. Перевірка: `W php artisan test --filter=ClickResource` зелений; `W php artisan route:list --path=admin/clicks` показує лише `index` і `view`.
- [ ] 5.2 (`task/02-redirect/campaign-delete-guard`) Кампанію з кліками не можна видалити: `DeleteAction::before()` в `EditCampaign` і `DeleteBulkAction::using()` + `reportBulkProcessingFailure()` у `CampaignsTable`, за зразком `EditOffer`/`OffersTable`; повідомлення радить вимкнути кампанію (spec campaigns, D10). Тести: сторінка редагування — кампанія з кліком лишається, є повідомлення; масове видалення двох кампаній, одна з кліками → видалено лише одну, звіт про невдачу; кампанія без кліків видаляється. Перевірка: `W php artisan test --filter=Campaign` зелений.

## 6. Навантажувальний тест (перевірка, не код)

- [ ] 6.1 [агент або користувач; після 1.1, 1.2 і груп 2–5] Перевірка «кілька сотень кліків за секунду, жоден не загубився» (D15). Кроки: (1) в адмінці створити окрему кампанію для тесту, запам'ятати її id і alias; `R HGET spread:config:campaigns <alias>` не порожній; (2) `R XLEN spread:clicks:dead` запам'ятати (очікується 0); (3) запустити воркер: `docker exec -it -u laradock -w /var/www/Spread laradock_all-workspace-85-1 php artisan spread:clicks:consume`; (4) з WSL: `hey -n 5000 -c 50 -q 6 -disable-redirects -host spread.local 'http://localhost/c/<alias>?zone=load&creative=1'` (~300 RPS); (5) дочекатися, поки `R XLEN spread:clicks` → 0 і `R XPENDING spread:clicks db` → 0; (6) `P "select count(*) from clicks where campaign_id = <id>"`. Перевірка: у звіті `hey` усі відповіді `302` і їх N = 5000; кількість кліків у БД = 5000; `XLEN spread:clicks:dead` не зріс. Записати під задачею фактичні RPS, p50/p99 латентності й час, за який воркер розібрав чергу. Цифри з WSL + Docker лише орієнтовні. Якщо були не-302 відповіді (502/504 через пул FPM) — кількість кліків у БД має дорівнювати кількості відповідей `302`; записати це й обговорити з користувачем.

## 7. Ручна перевірка «Готово, коли» (користувач)

- [ ] 7.1 [РУЧНО, користувач] Запустити воркер (як у 6.1, крок 3). У браузері Windows відкрити трекінгове посилання кампанії з 01-skeleton, підставивши значення замість макросів, наприклад `http://spread.local/c/<alias>?zone=8841&creative=17`. Перевірка: браузер одразу відкриває оффер, у його URL `sub1` — 26-символьний click_id, `sub2` = `8841`; в адмінці в розділі «Clicks» зверху цей клік: кампанія, пристрій `desktop`, ОС і браузер вашого ПК, на сторінці кліку `zone` → `8841`, `creative` → `17`. Країна порожня: з локальної машини IP — адреса Docker-мережі (приватна), і це очікувано.
- [ ] 7.2 [РУЧНО, користувач] Побачити реальну країну без підробки IP у редіректі: покласти в стрім штучне повідомлення з публічним IP. `W php artisan tinker --execute="echo Illuminate\Support\Str::ulid();"` → click_id; потім `R XADD spread:clicks '*' payload '{"v":1,"click_id":"<click_id>","ts":"2026-09-24T10:00:00.000Z","status":"ok","campaign_id":<id>,"offer_id":<offer_id>,"ip":"8.8.8.8","user_agent":"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/140.0 Safari/537.36","referer":null,"params":{"zone":"geo-test"}}'`. Перевірка: за кілька секунд в адмінці є клік із `zone` → `geo-test` і країною `US`.
- [ ] 7.3 [РУЧНО, користувач] Вимкнути тестову кампанію в адмінці й відкрити її посилання ще раз. Перевірка: браузер іде на `SPREAD_FALLBACK_URL`, у «Clicks» з'являється клік зі статусом `campaign_disabled`. Відкрити `http://spread.local/c/nosuchalias` → також fallback, нового кліку немає.
